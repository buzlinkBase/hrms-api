services: 
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: jhoncee2018/hrms-api:latest
    container_name: hrms-api
    restart: always
    ports:
      - "8082:8080"
    networks:
      - hrmsnet
    env_file:
      - .env
 
networks:
  hrmsnet:
    driver: bridge
